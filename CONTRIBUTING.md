## Contributing

### Development setup

- Python 3.11+
- Install deps: `pip install -r requirements.txt`
- Run locally: `python app.py`

### Pull requests

- Keep changes focused and small.
- Prefer adding/adjusting API docs in `docs/API.md` for behavior changes.
- Avoid committing runtime DB files (`data/*.db`).

